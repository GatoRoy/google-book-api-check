import { styled } from '@mui/material/styles';
import { Box } from 'components/controls/Box';

export const StyledLoginPageRow = styled(Box)`
  position: relative;
  width: 20rem;
  height: auto;
  padding: 0.5rem 1rem;
`;
